This guide will directly explain and help you with implementing `getcustomasset.lua`, `hookmetamethod.lua`, `getscriptclosure.lua`, `LoadLibrary.lua` and `getsenv.lua`
(This is a high level guide, it will not get into details about how to hook functions, for example. But it will provide the base for the lua function.)
